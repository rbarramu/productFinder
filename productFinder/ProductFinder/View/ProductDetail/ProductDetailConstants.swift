import UIKit

enum ProductDetailConstants {
    enum Texts {
        static let descriptionTitle = "Descripción"
    }

    enum Insets {
        static let estimatedRowHeight: CGFloat = 70
        static let tableViewInsets = UIEdgeInsets(top: 0, left: 0, bottom: 0, right: 0)
    }

    enum FontSize {
        static let title: CGFloat = 24
        static let originalPrice: CGFloat = 18
        static let price: CGFloat = 24
        static let descriptionTitle: CGFloat = 14
        static let description: CGFloat = 14
    }
}
