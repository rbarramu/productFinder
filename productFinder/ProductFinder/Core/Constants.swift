enum Constants {
    static let empty = ""
    static let space = " "
    static let clpSymbol = "$"

    enum Locale {
        static let chile = "es_CL"
    }

    enum Currency {
        static let chileanCurrency = "CLP"
    }
}
