import Foundation

enum DomainNetworkConstants {
    enum ApiHost {
        static let debug = "https://api.mercadolibre.com/"
        static let release = "https://api.mercadolibre.com/"
        static let stubbed = "http://localhost:8000/"
    }
}
